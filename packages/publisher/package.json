{
  "name": "@dep-mgr/publisher",
  "version": "0.0.0",
  "description": "A script to publish all package under specific directory to specific registry",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rollup --config rollup.config.ts --configPlugin @rollup/plugin-typescript && rimraf dist/types",
    "test": "jest"
  },
  "keywords": [
    "dependency",
    "packages",
    "publish",
    "registry"
  ],
  "author": "CH-Chang",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.21",
    "node-fetch": "^2.7.0",
    "p-limit": "^3.1.0",
    "ts-custom-error": "^3.3.1",
    "url-join": "^5.0.0"
  }
}
